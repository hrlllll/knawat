# Test code for knawat

- Run `yarn ` or `npm install` to get packages
- Run `docker-compose up` to start **elasticsearch**, **zookeper** , **kafka** and **jaeger**  
- Refer to the scripts in `package.json` for running services